# ScratchDAG 续接说明：分块 DAG 增加“权重 + 优先级”检查图

## 1. 当前已经完成的工作

### 1.1 新增算法

已经在 `pipeline` 段级调度链路中新增两个算法：

- `wcet_first`
- `t_level`

相关代码位置：

- `pipeline/algo/wcet_first.py`
- `pipeline/algo/t_level.py`
- `pipeline/algo/common.py`
- `pipeline/algo_registry.py`

当前算法注册结果已经可用，`python3 -m ScratchDAG pipeline list` 能列出：

- `cpf`
- `heft`
- `lpf`
- `t_level`
- `wcet_first`
- `zhao2020`

### 1.2 权重口径统一

段级调度算法已经统一读取 `pipeline/timing/.../timing.json` 中每个 segment 的 `avg_ns`。

当前约定：

- `pipeline/timing` 默认运行 10 次
- `timing.json` 中保留 `repeats`
- 所有段级算法统一使用 `avg_ns`

相关代码位置：

- `pipeline/timing.py`
- `pipeline/timing_config.py`
- `pipeline/algo/common.py`
- `pipeline/algo/lpf.py`
- `pipeline/algo/cpf.py`
- `pipeline/algo/heft.py`
- `pipeline/algo/zhao2020.py`

### 1.3 项目改名兼容

项目已经从 `mycallyplus_v1` 迁移到 `ScratchDAG`。本轮已经修复了会影响运行的关键链路：

- 包入口兼容
- GUI / 子进程启动模块名
- level1 / level2 / pipeline 的关键导入
- 默认 `base-dir`
- 部分实验脚本的 repo-root 检测

关键文件：

- `__init__.py`
- `launch.py`
- `runtime_env.py`
- `ui/gui.py`
- `ui/generation_gui.py`
- `pipeline/*.py`
- `level1/*.py`
- `level2/*.py`

验证过的命令：

```bash
cd /home/chove/Desktop
python3 -m ScratchDAG pipeline list
python3 ScratchDAG/launch.py pipeline list
python3 -m ScratchDAG generate --help
```

## 2. 已完成的正确性检查

已经对 `zhang1` 的以下两套插桩结果做过人工核对：

- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/instrument/level2/effective_line_merge/t_level`
- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/instrument/level2/effective_line_merge/wcet_first`

核对结论：

### 2.1 `wcet_first`

`schedule.json` 与 `timing.json.weights[*].avg_ns` 完全一致。

即：

- 分数 = `avg_ns`
- 按分数降序排序
- 同分按 `seg_id` 升序稳定打破平局
- 再映射到 `99..1`

### 2.2 `t_level`

`schedule.json` 与按 `dag_seg.json` 重算得到的前向最长路分数完全一致。

定义：

```text
t_level(v) = max_{u in pred(v)} (t_level(u) + cost(u))
source 节点 = 0
cost = avg_ns
```

同样是：

- 先按 `t_level score` 降序
- 同分按 `seg_id` 升序
- 再映射到 `99..1`

### 2.3 插桩位置

已检查 `source_instrumented.c` 中 `l1_set_thread_prio_fifo(...)` 的插入值，与：

- `segments.json` 的 `start_line`
- `schedule.json.priorities`

是对齐的，没有发现“优先级算对但回写错段”的问题。

## 3. 用户当前的新需求

用户希望在现有“分块 DAG”基础上，多生成一张图，便于人工检查：

- 每个节点显示该段的权重
- 每个节点显示该算法分配的优先级
- 每个节点显示该段在源码中对应的常量名（如 `C1`）

目标是能直接看一张图完成人工核对，而不是分别打开：

- `dag_seg.json`
- `timing.json`
- `schedule.json`
- 源码文件（查 `busy_wait_seconds(Cx)`）

### 3.1 新增的“常量名绑定”要求

用户新增目标：在 `validation`（以及/或 `schedule`）输出的带注释 DAG 中，节点应能直接看到常量名，形成以下可追溯关系：

`segment(seg_id) -> const_name(Cx) -> avg_ns -> prio`

约束说明：

- 常量名来自源码中的 `busy_wait_seconds(Cx)` 调用。
- 绑定粒度是“分块节点（segment）”。
- 若某分块内没有 `busy_wait_seconds(Cx)`，则常量名记为 `NA`（或 `-`），但不能静默丢失该节点。
- 若同一分块内出现多个常量（理论上应避免），应显式报错，防止错误绑定。

当前前提：

- `zhang1` / `zhang2` / `zhang3` / `zhang22` 已整理为按出现顺序连续编号（`C1..Cn`），便于稳定绑定。

## 4. 建议下一个窗口继续做的事情

### 4.1 目标图的建议内容

建议新增一张 DOT / PNG：

- 输入：
  - `blocks/<level>/<rule>/dag_seg.json`
  - `timing/<level>/<rule>/timing.json`
  - `schedule/<level>/<rule>/<algo>/schedule.json`
- 输出：
  - `schedule/<level>/<rule>/<algo>/dag_seg_annotated.dot`
  - `schedule/<level>/<rule>/<algo>/dag_seg_annotated.png`

建议每个节点标签至少包含：

- `seg_id`
- `const_name`
- `avg_ns`
- `prio`

例如：

```text
MU:main#007@222-231
const=C12
avg_ns=19980
prio=94
```

如果空间允许，也可以加：

- `function`
- `kind`

但优先级检查最关键的是：

- 节点 ID
- 常量名
- 权重
- 优先级

### 4.2 最合适的接入点

最建议接在 `pipeline/schedule.py` 这一层。

原因：

- `run_schedule()` 同时已经拿到了：
  - `dag_json`
  - `segments_json`
  - `timing_json`
  - `schedule_json`
- 图的输出目录正好也是：
  - `中间结果/<base>/pipeline/schedule/<level>/<rule>/<algo>/`

不建议接到 `pipeline/instrument.py`，因为那一层已经是“源码回写”阶段，图本质上属于“调度结果可视化”。

### 4.3 建议新增的实现位置

建议新增一个小工具模块，例如：

- `pipeline/schedule_render.py`

建议提供一个函数，例如：

```python
def render_annotated_schedule_dag(
    *,
    dag_json: Dict,
    segments_json: Dict,
    timing_json: Dict,
    schedule_json: Dict,
    source_text: str,
) -> str:
    ...
```

输出 DOT 字符串。

然后在 `pipeline/schedule.py` 中：

1. 生成 `schedule.json`
2. 调用上面的 render 函数
3. 写出：
   - `dag_seg_annotated.dot`
   - `dag_seg_annotated.png`

渲染 PNG 可复用 `pipeline/runner.py` 里 `_render_dot()` 的思路，或者直接在 `pipeline/schedule.py` 里用 `dot -Tpng`。

### 4.4 建议的节点标签格式

建议使用 Graphviz `label` 多行文本：

```text
{seg_id}\nconst={const_name}\navg_ns={avg_ns}\nprio={prio}
```

注意：

- `const_name` 来自该 segment 覆盖源码行中的 `busy_wait_seconds(Cx)` 提取结果
- `avg_ns` 来自 `timing_json["weights"][seg_id]["avg_ns"]`
- `prio` 来自 `schedule_json["priorities"][seg_id]`
- 缺字段时要显式报错，不要静默跳过

### 4.5 常量名绑定建议实现

建议在 `schedule_render.py` 新增一个绑定步骤（可独立函数）：

1. 根据 `segments.json` 读取每个节点的 `start_line/end_line`。
2. 在源码对应行区间内匹配 `busy_wait_seconds(C\d+)`。
3. 生成映射：`seg_id -> const_name`。
4. 将 `const_name` 注入节点标签，并可选写入 `validation/const_binding.json` 便于调试。

推荐输出（新增）：

- `中间结果/<base>/pipeline/validation/<level>/<rule>/<algo>/const_binding.json`

建议结构：

```json
{
  "MU:main#007@222-231": {"const_name": "C12", "line": 224},
  "SEG:worker_c2#002@103-103": {"const_name": "NA", "line": null}
}
```

### 4.6 建议的验证对象

先对这两组已知样例验证：

- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/t_level/`
- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/wcet_first/`

期待产物：

- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/t_level/dag_seg_annotated.dot`
- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/t_level/dag_seg_annotated.png`
- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/wcet_first/dag_seg_annotated.dot`
- `/home/chove/Desktop/ScratchDAG/中间结果/zhang1/pipeline/schedule/level2/effective_line_merge/wcet_first/dag_seg_annotated.png`

## 5. 下一个窗口可以直接复用的检查结论

如果下一个窗口需要确认算法是否已经正确，可直接复用以下事实：

- `wcet_first` 当前实现是正确的
- `t_level` 当前实现是正确的
- `zhang1` 这组产物上，算法分数、优先级映射、源码插桩三者一致

因此下一步重点不是“再证明算法对不对”，而是：

- 把调度结果直接可视化成一张带 `const_name + avg_ns + prio` 的段级 DAG 图

## 6. 本窗口没有继续做的事情

本窗口没有开始实现新图的生成，只完成了：

- 对 `t_level` / `wcet_first` 的正确性核查
- 生成本续接文档

所以下一个窗口可以直接进入实现阶段，不需要先回头重新分析算法逻辑。


# zhang1 五算法 runtime 与程序体内手动测时校验（ws=100）

## 1. 实验目的

验证 `runtime_compare` 工具在 `work_scale=100` 下，对 zhang1 的全部 5 个调度算法（cpf、heft、zhao2020、wcet_first、t_level）的测时结果是否与程序体内手动计时口径一致。

## 2. 两种测时方法说明

### 2.1 runtime_compare 工具测时（wrap_main 机制）

原理：

1. 编译时使用 `-Wl,--wrap=main`，将 `main()` 入口重定向到 `__wrap_main()`
2. `__wrap_main()` 中调用 `l1_prog_begin()` 记录起始时间戳
3. 然后调用 `__real_main()`（即原始 `main()`）
4. 通过 `atexit(l1_prog_end)` 注册退出回调
5. 程序退出时 `l1_prog_end()` 计算耗时，输出 `PROGRAM_TOTAL_NS=<纳秒>` 到 stderr
6. Python 端从 stderr 解析该值

涉及文件：

- `level1/wrap_main.c` — `__wrap_main()` 入口包装
- `level1/prog_timer.c` — `l1_prog_begin()` / `l1_prog_end()`，使用 `clock_gettime(CLOCK_MONOTONIC_RAW)`
- `level1/prog_timer.h` — 头文件
- `tools/runtime_compare/utils/time_parse.py` — 解析 `PROGRAM_TOTAL_NS=(\d+)`

编译命令：

```bash
gcc -O0 -g -std=c11 -pthread -Wl,--wrap=main -lm \
    -I<src_dir> -I<level1> -DWORK_SCALE=<ws> \
    source_original.c wrap_main.c prog_timer.c -o app_baseline
```

### 2.2 程序体内手动测时（inject_main_timer）

原理：

1. 在 `main()` 函数体的第一行注入 `uint64_t __prog_t0_ns = my_now_ns();`
2. 在 `return 0;` 之前注入 `fprintf(stderr, "PROGRAM_TOTAL_NS=%llu\n", ...);`
3. `my_now_ns()` 使用 `clock_gettime(CLOCK_MONOTONIC_RAW)` 获取纳秒时间戳
4. 编译时不使用 `--wrap=main`，不链接 `wrap_main.c` / `prog_timer.c`
5. 直接编译运行，从 stderr 解析 `PROGRAM_TOTAL_NS`

注入的辅助函数：

```c
static uint64_t my_now_ns(void) {
  struct timespec ts;
#ifdef CLOCK_MONOTONIC_RAW
  clock_gettime(CLOCK_MONOTONIC_RAW, &ts);
#else
  clock_gettime(CLOCK_MONOTONIC, &ts);
#endif
  return (uint64_t)ts.tv_sec * 1000000000ULL + (uint64_t)ts.tv_nsec;
}
```

编译命令：

```bash
gcc -O0 -g -std=c11 -pthread -lm \
    -I<src_dir> -DWORK_SCALE=<ws> \
    source_original.c -o app_baseline
```

### 2.3 两种方法的区别

| 项目 | runtime_compare | 手动测时 |
|------|----------------|---------|
| 计时起点 | `__wrap_main()` 入口 | `main()` 函数体第一行 |
| 计时终点 | `atexit` 回调 | `return 0` 之前 |
| 时钟源 | `CLOCK_MONOTONIC_RAW` | `CLOCK_MONOTONIC_RAW` |
| 额外链接 | `wrap_main.c` + `prog_timer.c` | 无 |
| 链接器选项 | `-Wl,--wrap=main` | 无 |

理论上两者测量的时间范围几乎相同（都是 main 函数的执行时间），偏差应在 5% 以内。

## 3. 本次实验配置

- 测试用例：`zhang1`
- 算法：`cpf`、`heft`、`zhao2020`、`wcet_first`、`t_level`
- `work_scale = 100`
- `repeats = 10`（每个算法的 baseline 和 prio 各跑 10 次）
- `cpu_set = [0, 1]`
- `cores_per_task = 2`
- `use_sudo = false`

## 4. 实验流程

对每个算法（共 5 个），分别执行：

1. **手动测时**：
   - 取 `source_original.c`（baseline）和 `source_instrumented.c`（prio）
   - 注入 `my_now_ns()` 计时代码
   - 编译（不用 `--wrap=main`）
   - 绑核 [0,1] 运行 10 次
   - 收集 `PROGRAM_TOTAL_NS`

2. **runtime_compare 工具测时**：
   - 生成 JSON 配置文件
   - 调用 `tools/runtime_compare/main.py --cli --wait`
   - 从 `summary.json` 读取结果

3. **对比**：
   - 计算手动均值 vs runtime 均值的偏差百分比
   - 输出对比表格

## 5. 源文件路径

```
中间结果/zhang1/pipeline/instrument/level2/effective_line_merge/
├── cpf/
│   ├── source_original.c      (baseline)
│   └── source_instrumented.c  (prio)
├── heft/
│   ├── source_original.c
│   └── source_instrumented.c
├── zhao2020/
│   ├── source_original.c
│   └── source_instrumented.c
├── wcet_first/
│   ├── source_original.c
│   └── source_instrumented.c
└── t_level/
    ├── source_original.c
    └── source_instrumented.c
```

## 6. 预期产物

- `report.json` — 结构化原始数据
- `实验报告.md` — 可读的对比表格
- `manual_runs/` — 手动测时的编译产物和运行日志
- `runtime_results/` — runtime_compare 的输出

---

## 7. WORK_SCALE 对提升率的影响分析

**结论：WORK_SCALE 不影响提升率。**

原因：`WORK_SCALE` 是对所有节点统一乘的全局缩放系数，每个节点受到完全相同的影响，因此：

- 节点之间的相对权重关系不变
- 优先级计算的输入（各节点权重比例）不变
- 调度决策（执行顺序）不变
- baseline 和 prio 的时间同比缩放，提升率不变

真正影响调度结果的是**各节点的权重（weight）**，即节点之间的相对比例，而非 WORK_SCALE 这个全局系数。

因此，在只关心提升率的实验中，可以选择较小的 WORK_SCALE（如 10~20）以缩短实验时间，结果与 ws=100 等价。

---

## 8. 粗略对比实验方案（zhang1/2/3 × 5算法）

### 目标

快速验证五个算法在 zhang1、zhang2、zhang3 三个测试用例上的提升率，用于初步分析和横向对比。

### 实验参数

| 参数 | 值 |
|------|-----|
| 测试用例 | zhang1、zhang2、zhang3 |
| 算法 | cpf、heft、zhao2020、wcet_first、t_level |
| work_scale | 10（快速验证，不影响提升率） |
| repeats | 10 |
| cpu_set | [0, 1] |
| cores_per_task | 2 |

### 说明

- 共 3 × 5 = 15 组实验，每组跑 baseline + prio 各 10 次
- work_scale 选 10 是为了缩短实验时间，提升率结论与 ws=100 等价
- 结果重点关注各算法的提升率均值，用于横向对比算法优劣

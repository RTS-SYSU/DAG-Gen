```sh
请将该 DAG 的节点常量设计为“重干扰入口 + 轻关键入口 + 重主链 + 轻侧边门控”的结构，以便在2核、无通信开销、非抢占条件下，使FIFO显著劣于关键路径优先算法。

常量设置为：

C1  = 350
C2  = 8
C3  = 8
C4  = 220
C5  = 4
C6  = 1
C7  = 1
C8  = 1
C9  = 2
C10 = 140
C11 = 80
C12 = 100
C13 = 50
C14 = 80

节点映射：
C1=worker_c2#001
C2=worker_c2#002
C3=worker_c2#003
C4=worker_c1#001
C5=worker_c1#002
C6=worker_c0#001
C7=main#001
C8=main#002
C9=main#003
C10=main#004
C11=main#005
C12=main#006
C13=main#007
C14=main#008

设计目标：
1. C4 和 C1 继续作为重入口干扰，其中 C1 进一步加重，尽量让 FIFO 在入口阶段就浪费一个核心。
2. C6、C8、C9 仍然保持极轻，保证优先级调度时能尽快释放主链。
3. 缩短 C10→C11→C12→C13→C14 主链尾部，让“入口阶段的错误调度”在总 makespan 中占更大比例。
4. 将 C4+C5 控制在接近 `C7+C8+C9+C10+C11`，让 `worker_c1` 在 prio 下大体可被主链遮住，但在 FIFO 下更容易外露。
5. 将 C1+C2+C3 控制在接近 `C7+C8+C9+C10+C11+C12+C13`，让 `worker_c2` 在 prio 下尽量被遮住，而在 FIFO 下成为后段额外拖尾。
6. 这样会把差异集中到“根节点后的前两次核心分配”，使 FIFO 的效果更差。
```
